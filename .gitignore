# Never commit secrets
.env

# The corpus lockfile ships absent — the trusted author generates it locally
memory/corpus/.corpus.lock

__pycache__/
*.pyc

# Local Python environment created by the start scripts
.venv/
